package ProduccionAgricola;

public class Main {

    public static void main(String[] args) {
/*
        // Crear fruta
        Fruta manzana = new Fruta("Manzana", 50, 2000, 3500);

        // Crear periodos
       // PeriodoCosecha primavera = new PeriodoCosecha("Primavera 2026", 10);
        //PeriodoCosecha verano = new PeriodoCosecha("Verano 2026", 8);

        // Agregar periodos
        manzana.agregarPeriodo(primavera);
        manzana.agregarPeriodo(verano);

        // Mostrar información
        for (PeriodoCosecha p : manzana.getPeriodos()) {

            System.out.println("Periodo: " + p.getNombrePeriodo());

            double produccion = p.calcularProduccionTotal(
                    manzana.getExtensionHect());

            double costo = p.calcularCostoTotal(
                    manzana.getExtensionHect(),
                    manzana.getCostoProduccionPromedioTon());

            double ganancia = p.calcularGananciaEstimada(
                    manzana.getExtensionHect(),
                    manzana.getCostoProduccionPromedioTon(),
                    manzana.getPrecioVentaPromedioTon());

            System.out.println("Producción total (ton): " + produccion);
            System.out.println("Costo total: $" + costo);
            System.out.println("Ganancia estimada: $" + ganancia);
            System.out.println("----------------------------");
        }*/
    }
}